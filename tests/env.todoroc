app [main!] { pf: platform "../platform/main.roc" }

import pf.Stdout
import pf.Env

main! = |_args| {
    Stdout.line!(
        \\Testing Env module functions...
        \\
        \\Testing Env.cwd!:
    )?
    #cwd = Env.cwd!({})?
    # Stdout.line!(
    #     \\cwd: ${Path.display(cwd)}
    #     \\
    #     \\Testing Env.exe_path!:
    # )?
    # exe_path = Env.exe_path!({})?
    # Stdout.line!(
    #     \\exe_path: ${Path.display(exe_path)}
    # )?

    Stdout.line!(
        \\All tests executed TODO.
    )?

    Ok({})
}